spring:
  application:
    name: posting-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:postingdb}
    username: ${DB_USER:posting}
    password: ${DB_PASSWORD:posting}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          secret: ${APP_SECURITY_JWT_SECRET:this_is_a_development_jwt_secret_that_is_long_enough_please_change_in_prod_1234567890}

server:
  port: ${PORT:8082}

springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /swagger

app:
  security:
    jwt:
      issuer: ${JWT_ISSUER:ledgerpay}
      secret: ${JWT_SECRET:dev-secret-change}
      access-token-ttl: PT15M

management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      probes:
        enabled: true
